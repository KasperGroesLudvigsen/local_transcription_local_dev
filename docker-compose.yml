version: '3.8'

services:
  transcription-api:
    build: .
    ports:
      - "3030:3030"
    volumes:
      - ./data:/app/data  # Mount for audio files if needed
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    environment:
      - PYTHONPATH=/app
      - CUDA_VISIBLE_DEVICES=0
      - HF_TOKEN=${HF_TOKEN}
    stdin_open: true
    tty: true